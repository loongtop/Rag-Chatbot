# Examples (CAF v0.6.5)

本目录提供端到端示例项目（Charter → L0/L1/L2 → Architecture → Specs → Code）。

- `minimal/`: 单模块、单语言（Python），包含可运行的 `src/` + `tests/`
- `medium/`: 中等复杂度（3 个模块），展示 L0/L1/L2/Interface/Architecture/Specs 的典型产物
- `enterprise/`: 多组件/多语言 Charter 示例（components）+ 推荐目录布局

