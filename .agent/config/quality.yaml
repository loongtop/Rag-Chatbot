# =============================================================================
# ⚠️ DEPRECATED - Quality Standards Configuration (Legacy)
# =============================================================================
#
# DEPRECATED: 此文件已弃用，仅保留用于向后兼容（提供默认门禁阈值与迁移指引）。
#
# 新项目应使用语言配置文件（由 `charter.yaml` 的 `language_profile` 或 `components[].language_profile` 选择）：
# - `.agent/config/quality.python.yaml`
# - `.agent/config/quality.java.yaml`
# - `.agent/config/quality.cpp.yaml`
# - `.agent/config/quality.swift.yaml`
#
# 计划：此文件将在 v1.0.0 移除。
# =============================================================================

version: "0.1"
deprecated: true
removed_in: "v1.0.0"

replaced_by:
  python: ".agent/config/quality.python.yaml"
  java: ".agent/config/quality.java.yaml"
  cpp: ".agent/config/quality.cpp.yaml"
  swift: ".agent/config/quality.swift.yaml"

defaults:
  test_coverage_minimum: 95
  complexity_limit: 10
